{
    "network": {
        "servers": {{ logstash_servers }},
        "ssl key": "/etc/logstash-forwarder/ssl/lumberjack.key",
        "ssl certificate": "/etc/logstash-forwarder/ssl/lumberjack.crt",
        "ssl ca": "/etc/logstash-forwarder/ssl/lumberjack.crt",
        "timeout": {{ logstash_timeout }}
    }
}
